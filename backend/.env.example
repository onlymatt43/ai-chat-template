# Minimal example environment for backend (only vars used by code)
PORT=3000

# OpenAI keys (used by chat endpoints)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Chat preset
CHAT_PRESET=generic

# WAN 2.2 (video provider)
WAN_API_KEY=
WAN_API_URL=
VIDEO_PROVIDER=wan

# Backblaze B2 placeholders (10GB free):
B2_KEY=
B2_SECRET=
B2_ENDPOINT=https://s3.us-east-005.backblazeb2.com
B2_BUCKET=
